/public
resources/
node_modules/

.hugo_build.lock
*.bak
# Local discovery smoke output (tools/cve_discovery.py default --out).
# CI writes to .github/cve-discovery-report.md instead (see cve-review.yml).
cve-discovery-report.md
.doc-style-anchor.md
.change-manifest.md
AGENTS.md

# Temporary build, verification and cache directories
# (Hugo output copies, htmltest link checker, manual verify builds)
/tmp/
/tmp-*/

# Privacy firebreak: private CEO vaults must never enter product repos
notes-vault/
email-vault/
raw-achive*/
raw-archive
**/notes-vault/
**/email-vault/

# htmltest binary built on demand by tools/check-links.sh
tools/bin/
