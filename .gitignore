/public
resources/
node_modules/

.hugo_build.lock
*.bak
# Local discovery smoke output (local discovery default --out).
# CI writes to .github/cve-discovery-report.md instead (see the discovery workflow).
cve-discovery-report.md
.doc-style-anchor.md
.change-manifest.md
AGENTS.md

# Temporary build, verification and cache directories
# (Hugo output copies, htmltest link checker, manual verify builds)
/tmp/
/tmp-*/

# Local checkout names that must not enter this repo
notes-vault/
email-vault/
raw-achive*/
raw-archive
**/notes-vault/
**/email-vault/

# htmltest binary built on demand by tools/check-links.sh
tools/bin/
