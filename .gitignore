/public
resources/
node_modules/

.hugo_build.lock
*.bak
# Local discovery smoke output at repo root only (local discovery default --out).
# Leading slash = root only; .github/cve-discovery-report.md stays trackable for CI.
/cve-discovery-report.md
.doc-style-anchor.md
.change-manifest.md
AGENTS.md

# Temporary build, verification and cache directories
# (Hugo output copies, htmltest link checker, manual verify builds)
/tmp/
/tmp-*/

# Local checkout names that must not enter this repo
notes-vault/
email-vault/
raw-achive*/
raw-archive
**/notes-vault/
**/email-vault/

# htmltest binary built on demand by tools/check-links.sh
tools/bin/
