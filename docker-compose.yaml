services:
  site:
    image: hugomods/hugo:exts
    command: server --bind 0.0.0.0 --port 1313 --logLevel info
    ports:
      - '1313:1313'
    volumes:
      - .:/src
      - ~/.cache/hugo_module:/root/.cache/hugo_module
